//! 数据转换服务

pub struct DataTransform;

impl DataTransform {
    pub fn pcap_to_geojson(_data: &[u8]) -> Vec<u8> {
        // 实现PCAP到GeoJSON的转换
        vec![]
    }

    pub fn pcap_to_mvt(_data: &[u8]) -> Vec<u8> {
        // 实现PCAP到MVT的转换
        vec![]
    }
}